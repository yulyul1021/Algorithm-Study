class Solution {
    public boolean solution(int x) {
        boolean answer = true;
        int y = x/10 + x%10;
        
        answer = (x%y==0) ? true : false;          
        
        return answer;
    }
}
