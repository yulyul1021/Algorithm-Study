class Solution {
    public String solution(String[] seoul) {
        String pe = "Kim";
        int x = 0;
        for(int i=0; i<seoul.length; i++){
            if(pe.equals(seoul[i])) {x=i; break;}
        }
        String answer = "김서방은 "+x+"에 있다";
        return answer;
    }
}
